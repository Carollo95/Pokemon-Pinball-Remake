const FIELD_BONUS = {
    MOLE : "mole",
    GHOST: "ghost",
    CAT: "cat",
    SEAL: "seal",
    CLONE: "clone",
}

const TRAVEL_DIRECTION={
    LEFT: 'left',
    RIGHT: 'right',
}

class Field extends Stage{
    constructor(status){
        super(status);
    }
    
}